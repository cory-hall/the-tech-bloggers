<div class="card">
    <div class="card-header flex-row dash-post">
        <h4><a class="link" href="/dashboard/edit/{{id}}">{{title}}</a></h4>
        <p class="post-date">{{format_date created_at}}</p>
    </div>
</div>