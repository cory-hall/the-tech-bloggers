<div class="card">
    <div class="card-header flex-row dash-post">
        <h4><a class="link" href="/post/{{id}}">{{title}}</a></h4>
        <p class="post-date">Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
        <p class="card-body">{{content}}</p>
</div>